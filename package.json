{
  "name": "jsclaw-agent-micro",
  "version": "0.0.2",
  "description": "Zero-dependency agent runner for jsclaw containers. Pure JavaScript ESM, Anthropic API over fetch.",
  "type": "module",
  "main": "./src/index.js",
  "license": "AGPL-3.0-or-later",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jsclaw/agent-micro.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "lint": "node --check runner.js && node --check src/api.js && node --check src/tools.js && node --check src/jsclaw-tools.js && node --check src/loop.js && node --check src/mcp.js && node --check src/index.js",
    "docker:build": "docker build -t jsclaw-agent-micro:latest -f container/Dockerfile ."
  },
  "keywords": ["jsclaw", "agent", "anthropic", "claude", "zero-dependency"],
  "dependencies": {}
}
